# Query: .max(
# ContextLines: 1

2 results - 1 file

sidereal-visual-test/src/entities.rs:
  45      // Ensure we have a minimum size to avoid empty ranges
  46:     let bounds = sim_args.size.max(20) as f32;
  47      

  68          let min_distance = 5.0;
  69:         let max_distance = (bounds / 2.0 - 5.0).max(min_distance + 1.0);
  70          let orbital_distance = rng.gen_range(min_distance..max_distance);
